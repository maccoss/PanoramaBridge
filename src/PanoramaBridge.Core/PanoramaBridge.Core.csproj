<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>PanoramaBridge.Core</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Internal helpers are tested directly rather than being made public just to reach them. -->
    <InternalsVisibleTo Include="PanoramaBridge.Tests" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Data.Sqlite" Version="8.0.30" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="8.0.3" />
  </ItemGroup>

  <ItemGroup>
    <!--
      The RAW check. Core depends on it and not the other way round: ThermoRaw has to stay
      buildable and shippable on its own.
    -->
    <ProjectReference Include="..\PanoramaBridge.ThermoRaw\PanoramaBridge.ThermoRaw.csproj" />
  </ItemGroup>

</Project>
